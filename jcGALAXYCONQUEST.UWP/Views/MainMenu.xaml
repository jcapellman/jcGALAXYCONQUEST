<Page
    x:Class="jcGALAXYCONQUEST.UWP.Views.MainMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:jcGALAXYCONQUEST.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource MainGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Style="{StaticResource MainMenu_Header}" Text="GALAXY CONQUEST" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Style="{StaticResource MainMenu_Buttons}" Content="new game" />
            <Button Grid.Row="1" Style="{StaticResource MainMenu_Buttons}" Content="load game" />
            <Button Grid.Row="2" Style="{StaticResource MainMenu_Buttons}" Content="about" />
        </Grid>
    </Grid>
</Page>
